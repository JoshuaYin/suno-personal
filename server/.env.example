# MySQL
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=
DB_PASSWORD=
DB_DATABASE=suno_personal

# JWT
JWT_SECRET=
JWT_ACCESS_EXPIRES=3h
JWT_REFRESH_EXPIRES=7d

# AES-256-GCM 加密密钥 (hex, 64 chars = 32 bytes, 用 openssl rand -hex 32 生成)
ENCRYPT_KEY=

# 上游 API 地址
API_BASE=https://api.modelverse.cn
